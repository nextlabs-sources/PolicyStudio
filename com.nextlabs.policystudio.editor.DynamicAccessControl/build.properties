source.. = src/
output.. = bin/
bin.includes = META-INF/,\
               .,\
               plugin.xml

common.build.script.dir=${basedir}/../build_scripts
build.dir=${basedir}/../build
plugin.dir=plugins
policy.studio.plugin=com.nextlabs.policystudio.editor.DynamicAccessControl
policy.studio.plugin.dir=${plugin.dir}/${policy.studio.plugin}
build_project_dependencies.build.basedirlist=../com.nextlabs.policystudio
build.main.classes.dir=${build.dir}/main/classes/${policy.studio.plugin}

# Policy Studio has a different directory structure due to Eclipse IDE integrati
on
src.main.dir=${basedir}/src
src.main.resource.dir=${basedir}/src
