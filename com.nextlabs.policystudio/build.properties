source.. = src/
output.. = bin/
bin.includes = .,\
               plugin.xml,\
               policystudio.jar,\
               schema/,\
               splash.bmp,\
               resources/,\
               META-INF/
source.policystudio.jar = src/
src.includes = src/,\
               splash.bmp,\
               schema/,\
               resources/,\
               policystudio.product,\
               plugin.xml,\
               config/,\
               build.properties,\
               META-INF/

common.build.script.dir=${basedir}/../build_scripts
build.dir=${basedir}/../build
plugin.dir=plugins
policy.studio.plugin=com.nextlabs.policystudio
policy.studio.plugin.dir=${plugin.dir}/${policy.studio.plugin}
build.main.classes.dir=${build.dir}/main/classes/${policy.studio.plugin}


# Policy Studio has a different directory structure due to Eclipse IDE integration
src.main.dir=${basedir}/src
src.main.resource.dir=${basedir}/src
